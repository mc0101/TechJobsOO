@model TechJobs.Models.Job


<!--/ TODO #2.1- declare the ViewModel (outside of this @{ } block)
}-->


           <table class="job-listing">
               
                   <tr><h3>@Model.Name</h3></tr>
                   <tr>Employer: @Model.Employer</tr><br />
                   <tr>Location: @Model.Location</tr><br />
                   <tr>Core Competency: @Model.CoreCompetency</tr><br />
                   <tr>Position Type: @Model.PositionType</tr><br />
               
               @{

                   <!--// TODO #2.2 - display the job fields, one per row (outside of this @{ } block)-->
               }
           </table>